# Find Your 7 Twins — GKT

This is the existing Find Your 7 Twins frontend integrated under the GKT site at `/twins/`.

Before publishing, set `API_BASE` in `config.js` to the real HTTPS URL of the deployed FastAPI backend. Do not commit secrets or private backend credentials.
